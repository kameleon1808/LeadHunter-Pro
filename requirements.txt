# LeadHunter Pro — pinned dependencies
# Install: pip install -r requirements.txt
# After Playwright install: playwright install chromium

# Database
SQLAlchemy==2.0.48

# Web scraping
playwright==1.58.0
aiohttp==3.13.3
beautifulsoup4==4.14.3

# AI enrichment
# Primary engine: Claude Code CLI (claude -p) — no API key required
# anthropic>=0.20.0  # Optional: install only if ANTHROPIC_API_KEY is set

# Export
openpyxl==3.1.5

# Terminal UI
rich==14.3.3

# Configuration
python-dotenv==1.2.2

# Testing
pytest==9.0.2
pytest-asyncio==1.3.0
